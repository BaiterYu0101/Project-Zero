## Here to start to write my first deployment file in my Zero_Project

apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-api-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: test-api
  template: 
      metadata: 
        labels:
	  app: test-api
      spec: 
	    containers:
	      - name: main-container
	      image: ghcr.io/BaiterYu0101/test-api: latest
	      ports:
	      - containerPort: 8000
          

