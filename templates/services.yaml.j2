apiVersion: v1
kind: Service
metadata:
  name: {{ app_name }}-service
spec:
  selector:
    app: {{ app_name }}
  type: {{service_type}}
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ app_port }}